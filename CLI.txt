1) mkdir .filename to create new HIDDEN FILE. Then if we write in the right directory 'ls -a' we can see all documents in the CLI
2) mkdir -p /c/Users/myusername/these/filders/are/just/for/fun to create multipe nested directories
3) echo -e 'first message \n second message' to append a message to a file,without newline chracter
